// warning! these are protocol vars that need to agree on both the client and server
export const CHUNK_SIZE = 12000;
export const CHUNK_TIMEOUT_MS = 30000;
export const MAX_TOTAL_CHUNKS = 1000;
export const MAX_SERIALIZED_SIZE = 12000000;